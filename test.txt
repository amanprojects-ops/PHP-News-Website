<?php 

 include "config.php";

 for ($i = 0; $i < 90000; $i++) {
    $randomIP = generateRandomIP();

    $insertQuery = "INSERT INTO visitors (ip) VALUES ('$randomIP')";
     mysqli_query($conn,$insertQuery);
 }


 function generateRandomIP() {
    $ip = array();
     for ($i = 0; $i < 4; $i++) {
         $ip[] = mt_rand(0, 255);
    }
    return implode('.', $ip);
 }
?>